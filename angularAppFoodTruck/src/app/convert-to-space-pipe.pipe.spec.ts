import { ConvertToSpacePipePipe } from './convert-to-space-pipe.pipe';

describe('ConvertToSpacePipePipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertToSpacePipePipe();
    expect(pipe).toBeTruthy();
  });
});
